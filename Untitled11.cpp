/* comparep.c */
/* campares word typeed in with word in program */
/* uses pointers */
main()
{
 int dex;
 int enter =0;
 char name[40];
 static char * [ist[MAX] =
                 { "KATRINA,"
                   "NIGEL",
                   "ALISTAIR",
                   "FRANCESCA",
                    "GUSTAV",};
printf("Enter your name:  ");        /* get name */
gets(name);
for (dex=0; dex< MAX; dex++)         /* go thru list */ 
   if(strcmp(list[dex],name)==0 )    /* if match */
      enter =1;                      /* set flag */
      if (enter ==1)                  /* if flag set */
      printf ("you may enter,oh honored one".); /* one reponse */
      else                              /* otherwise */
       printf("guards: Remove this person : ");  /* different one */
   } 
      
               
				
				
				
				 
